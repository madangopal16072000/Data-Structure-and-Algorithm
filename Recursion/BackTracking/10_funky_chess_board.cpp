#include<iostream>
using namespace std;

bool move(int cell[][11], int i, int j, int N)
{
    if(cell[i][j] != 1 && cell[])
}
int main()
{
    int N;
    cin >> N;
    int cell[11][11];
    for(int i=0; i<N; i++)
    {
        for(int j=0; j<N; j++)
        cin >> cell[i][j];
    }

    move(cell, 0, 0, N);
    return 0;
}